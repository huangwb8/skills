# 优化计划（A3）: 版本号同步与配置口径补齐

**计划ID**: v202601121153  
**目标**: 将版本号作为“活语义接口”保持一致（`config.yaml` ↔ `SKILL.md` ↔ 文档说明），避免后续迭代出现漂移。  
**范围**: `auto-test-skill/config.yaml`、`auto-test-skill/SKILL.md`、`auto-test-skill/README.md`

---

## 问题清单（按优先级）

### P0（必须修复）

1) **版本号未随代码/模板/脚本变更同步更新**
- **位置**:
  - `auto-test-skill/config.yaml`（`skill_info.version`）
  - `auto-test-skill/SKILL.md`（YAML frontmatter `version`）
- **影响**: 使用方无法通过版本号判断行为变化；也会违背本项目“版本单一事实来源”的约定。
- **修复**: 版本号从 `2.0.0` 升级为 `2.0.1`，并同步到 `SKILL.md` 表头。
- **验证**: 仓库内无 `2.0.0` 残留；两处版本号一致。

### P1（强烈建议）

2) **README 未明确版本号口径**
- **位置**: `auto-test-skill/README.md` “配置说明”
- **影响**: 维护者不确定版本来源；容易出现重复维护与不一致。
- **修复**: 增加 `skill_info.version` 说明（版本号单一事实来源）。
- **验证**: README 明确指向 `config.yaml` 的版本字段。

---

## 执行步骤（按顺序）

1) 修改 `auto-test-skill/config.yaml` 与 `auto-test-skill/SKILL.md` 的版本号为 `2.0.1`  
2) 补充 `auto-test-skill/README.md` 配置说明中的版本字段口径  
3) 轻量测试与一致性检查（见本轮测试计划/报告）
