# 优化计划（A4）: CLI 体验改进 + 模板可用性增强

**计划ID**: v202601121154  
**目标**: 让 `create_test_session.py` 的 CLI 行为更符合“最小惊讶原则”，并让 `TEST_PLAN_TEMPLATE.md` 的关键字段可自动完成。  
**范围**: `auto-test-skill/scripts/create_test_session.py`、`auto-test-skill/templates/TEST_PLAN_TEMPLATE.md`

---

## 问题清单（按优先级）

### P1（强烈建议）

1) **非法参数导致 Python Traceback，错误提示不友好**
- **位置**: `auto-test-skill/scripts/create_test_session.py`
- **影响**: 用户/Agent 容易被 traceback 干扰；也不利于把“失败原因”沉淀到测试报告。
- **修复**: 对 `--skill-root`、`--kind`、`--id` 做 CLI 级错误输出（usage + 单行错误），退出码 2。
- **验证**: `--kind c` 输出为 usage + `error: ...`，无 traceback。

### P2（可选）

2) **`TEST_PLAN.md` 无法自动标注 A/B 轮，且关联文档路径偏冗长**
- **位置**:
  - `auto-test-skill/templates/TEST_PLAN_TEMPLATE.md`
  - `auto-test-skill/scripts/create_test_session.py`
- **影响**: 手工填充成本高；绝对路径降低可移植性。
- **修复**:
  - 模板新增 `{{ROUND_KIND}}`
  - `PLAN_DOC_PATH` 用相对路径（如 `plans/v*.md`）
- **验证**: 新会话的 `TEST_PLAN.md` 显示 `轮次类型: A轮/B轮`，且 `关联规划文档` 为相对路径。

---

## 执行步骤（按顺序）

1) 改造脚本输入校验：失败时输出友好的 CLI 错误信息  
2) 更新 `templates/TEST_PLAN_TEMPLATE.md`，并在脚本中补充 `ROUND_KIND`/相对路径替换  
3) 创建会话并验证产物（见测试计划/报告）
