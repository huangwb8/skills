# 优化计划（A5）: 冗余/误导项清理 + 回归验证

**计划ID**: v202601121155  
**目标**: 对前几轮改动做一次“冗余与误导项”清理，并补齐 A/B 轮脚本行为的回归验证证据。  
**范围**: `auto-test-skill/scripts/create_test_session.py`（模板渲染字段口径）

---

## 问题清单（按优先级）

### P1（强烈建议）

1) **B 轮报告模板中的“对应A轮测试”字段不应默认写成当前 B 轮 ID**
- **位置**: `auto-test-skill/scripts/create_test_session.py`（模板渲染字段 `A_TEST_ID`）
- **影响**: 会产生“看似填写但实际错误”的文档，误导复盘与追溯。
- **修复**: 脚本默认不替换 `{{A_TEST_ID}}`，让其保持占位符并提示维护者手动填入真实 A 轮 ID。
- **验证**: `plans/B轮-v202601121155.md` 中仍保留 `{{A_TEST_ID}}`（见本轮测试报告证据）。

---

## 执行步骤（按顺序）

1) 调整脚本模板渲染默认字段：移除对 `A_TEST_ID` 的默认替换  
2) 创建 A 轮会话与 B 轮会话用于回归验证（本轮以同一 `v202601121155` 作为样例 ID）  
3) 记录证据到 `tests/v202601121155/TEST_REPORT.md`
