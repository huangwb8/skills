# {项目名称} - 项目指令

你正在 `{工作目录}` 中工作：该目录用于 {项目用途}。

## 项目目标

{核心功能描述}

本工作区的核心目标是 {一句话概括项目的价值主张}。所有任务围绕 "{核心任务描述}" 展开。

## 核心工作流

当用户提出 {项目类型} 相关需求时，按以下流程执行：

### 1. 任务理解与确认

首先理解用户需求，然后：
{第一步工作流描述}

**关键原则**：
- {原则 1}
- {原则 2}
- {原则 3}

### 2. 执行流程

{核心执行步骤}

**强制要求**：{如有强制要求，在此描述}

**集成方式**：
- {描述与外部技能/工具的集成}
- {描述配置或调用方式}

### 3. 输出规范

{输出标准}

**默认格式**：{默认输出格式}

**质量标准**：{质量检查清单}

## 工程原则

本项目遵循以下工程原则：

| 原则 | 核心思想 | 在本项目中的体现 |
|------|----------|------------------|
| **KISS** | Keep It Simple, Stupid | {具体体现} |
| **YAGNI** | You Aren't Gonna Need It | {具体体现} |
| **DRY** | Don't Repeat Yourself | {具体体现} |
| **SOLID** | 面向对象设计五大原则 | {具体体现} |
| **关注点分离** | 不同层次逻辑应分离 | {具体体现} |
| **奥卡姆剃刀** | 如无必要，勿增实体 | {具体体现} |
| **最小惊讶原则** | API 行为应符合用户直觉 | {具体体现} |
| **早期返回原则** | 尽早返回，减少嵌套 | {具体体现} |

{如有项目特定原则，在此添加}

**原则冲突时的决策优先级**：
1. **正确性 > 一切**（{项目中的具体含义}）
2. **简洁性 > 灵活性**（{项目中的具体含义}）
3. **清晰性 > 性能**（{项目中的具体含义}）
4. **扩展性 > 紧凑性**（{项目中的具体含义}）

## 默认语言

除非用户明确要求其他语言，始终使用 {默认语言} 与用户对话与撰写文档/说明。

## 联网与搜索（如需）

默认优先使用项目内文件与本地上下文；确需联网获取信息时，优先使用本地 SearXNG（如已通过 MCP 配置）。仅当 SearXNG 不足以满足需求时再使用其它联网手段，并说明原因与保留关键链接。

## 目录结构

```
{目录树}
├── AGENTS.md          # 本文档：项目指令
├── CLAUDE.md          # Claude Code 特定指令
└── {项目相关目录}
```

### 目录说明

| 目录 | 用途 |
|------|------|
| `{目录名}` | {用途说明} |
| `{目录名}` | {用途说明} |

## 变更边界

- {修改范围限制 1}
- {修改范围限制 2}
- {保护性规则}

{如有多个项目并行的情况，在此说明}

## 有机更新原则

当需要更新本文档时：

### 1. 理解意图

首先理解用户需求背后的意图和在工作流中的本质作用

### 2. 定位生态位

每条规则/要求都应找到其在整个文档结构中的"生态位"——它与其他内容的关系、它服务的目标、它影响的其他部分

### 3. 协调生长

更新一个部分时，检查并同步更新相关部分：
- 更新工作流步骤时，同步更新示例和验证清单
- 更新输出规范时，同步更新引用该规范的其他章节
- 更新术语定义时，全局统一替换

### 4. 保持呼吸感

文档应该像生物体一样有"呼吸感"——章节之间有逻辑流动，而非割裂的清单

### 5. 定期修剪整合

当某个章节变得过于臃肿时，主动重构

## 更新时的自检清单

在响应任何"更新/优化/修改"请求时，问自己：

- [ ] **意图理解**：我真正理解了用户想要解决什么问题吗？
- [ ] **生态位定位**：这个更新应该放在文档的哪个位置？
- [ ] **影响范围**：这个更新会影响其他哪些章节？
- [ ] **冲突检查**：新内容是否与现有规则冲突？
- [ ] **术语一致性**：我是否使用了与文档其他部分一致的术语？
- [ ] **示例同步**：相关示例是否反映了新的规则？
- [ ] **内聚性保持**：更新后，该章节是否仍然围绕一个清晰的核心主题？
