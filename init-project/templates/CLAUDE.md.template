# {项目名称} - Claude Code 项目指令

你正在 `{工作目录}` 中工作：该目录用于 {项目用途}。

## Claude Code 环境特定说明

本文件保留用于 Claude Code 特定的配置和实践说明。通用项目指令见 AGENTS.md。

## 在 Claude Code 中触发任务

### 语义触发机制

Claude Code 使用**语义匹配**触发技能。当用户表述与相关技能的 `description` 匹配时，技能会被自动调用。

**有效触发示例**：
- "{触发示例 1}"
- "{触发示例 2}"
- "{触发示例 3}"

### 工作流

1. **理解用户需求** → {第一步描述}
2. **{第二步描述}** → {结果}
3. **{第三步描述}** → {结果}
4. **{第四步描述}** → {结果}

## 任务处理规范（Claude Code 实现）

在 Claude Code 中，当用户提出 {项目类型} 需求时：

### 决策流程

```python
# 伪代码：任务处理流程
def process_task(user_input: str, workspace: str) -> str:
    """
    根据用户输入处理任务

    规则：
    1. {规则 1}
    2. {规则 2}
    3. {规则 3}
    """
    # {处理逻辑}
    return result
```

**示例**：

| 用户输入 | 处理方式 |
|---------|----------|
| "{示例 1}" | {处理方式 1} |
| "{示例 2}" | {处理方式 2} |
| "{示例 3}" | {处理方式 3} |

## 技能调用方式

在 Claude Code 中，使用 `Skill` 工具：

```python
Skill(
    skill="{skill_name}",
    args="{参数说明}"
)
```

{如有多个技能需配合，在此说明调用关系}

## 文件引用规范

在 Claude Code 中引用文件时，使用 markdown 链接语法（注意：路径会根据实际生成的结构调整）：

- 文件：`[filename.tex](路径/filename.tex)`
- 特定行：`[filename.tex:42](路径/filename.tex#L42)`
- 行范围：`[filename.tex:42-51](路径/filename.tex#L42-L51)`
- 目录：`[目录名/](路径/目录名/)`

## Claude Code 特定验证要点

完成 {项目类型} 任务后，额外检查：

- [ ] **检查项 1**：{具体说明}
- [ ] **检查项 2**：{具体说明}
- [ ] **检查项 3**：{具体说明}
- [ ] **语言一致**：是否使用 {默认语言}（除非用户另有要求）？

## 与 AGENTS.md 的关系

- **AGENTS.md**：通用项目指令（Codex + Claude Code 共享）
- **CLAUDE.md**：Claude Code 特定补充

通用原则（工程原则、有机更新等）见 AGENTS.md。

## 多任务并行原则（Claude Code 实现）

{如项目支持多任务并行，在此说明}

**关键原则**：
- 🚫 **绝不干扰**现有任务，除非用户明确指定
- 🆕 每次新任务都创建新的 {任务隔离机制}
- 🔍 用户意图识别：
  | 用户输入 | 行为 |
  |---------|------|
  | "{示例 1}" | {行为 1} |
  | "{示例 2}" | {行为 2} |
  | "{示例 3}" | {行为 3} |

## 有机更新在 Claude Code 中的实践

当需要更新本文档时：

### 1. 理解意图

用户真正想解决什么问题？

### 2. 生态位定位

这个更新应该放在 CLAUDE.md 还是 AGENTS.md？
- Claude Code 特定 → CLAUDE.md
- 通用原则 → AGENTS.md

### 3. 协调更新

同步更新相关章节和交叉引用

### 4. 保持一致性

术语、示例与 AGENTS.md 保持统一

---

**Claude Code 特定提示**：
- 技能在会话启动时加载，修改后需新建会话生效
- 使用 markdown 链接语法引用文件可提升用户体验
- 遵循有机更新原则保持文档的内聚性
